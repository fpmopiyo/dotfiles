# Dotfiles

Version-controlled shell and editor config.

## What we added

This repo was created from resources listed below. Our contributions:

- **zshrc** — Zap plugin setup, Powerlevel10k theme, fzf key bindings (Ctrl+R history search, Ctrl+T file finder), fzf-tab completion, and sourcing logic for multiple fzf install paths (git, Debian, Arch)
- **install.sh** — Symlinks `zshrc` and `p10k.zsh` into `$HOME`, optional fzf install via apt
- **p10k.zsh** — Powerlevel10k config generated by `p10k configure`; versioned here and symlinked as `~/.p10k.zsh` so the wizard updates the repo file
- **README** — Setup instructions, wizard reference, font notes, license attribution

## How to view this README

- **Terminal:** `cat ~/.dotfiles/README.md` or `less ~/.dotfiles/README.md`
- **Cursor/VS Code:** File → Open File… → `~/.dotfiles/README.md`, or open the folder `~/.dotfiles` and click README.md

## Contents

- **zshrc** — Zsh config (Zap, Powerlevel10k, fzf, history search)
- **p10k.zsh** — Powerlevel10k prompt config (from `p10k configure`); symlinked as `~/.p10k.zsh`

## Setup

1. Symlink into `$HOME` (already done if you ran `install.sh`):
   ```bash
   ln -sf ~/.dotfiles/zshrc ~/.zshrc
   ```
2. Install fzf (for Ctrl+R history search and tab completion):
   - Debian/Ubuntu: `sudo apt install fzf`
   - Or from source: <https://github.com/junegunn/fzf#installation>
3. Restart Zsh: `exec zsh`
4. Run the Powerlevel10k wizard (see below).

## Powerlevel10k setup wizard

The wizard customizes prompt style, icons, and layout. You can run it anytime:

```bash
p10k configure
```

- **First time:** It often starts automatically after you open a new Zsh session. If not, run the command above.
- **Later:** Run `p10k configure` again to change style, colors, or segments.
- **Font:** For the full set of styles and icons, install [MesloLGS Nerd Font](https://github.com/romkatv/powerlevel10k#fonts) and set your terminal font to “MesloLGS NF” before running the wizard.

## Font (Powerlevel10k)

For full Powerlevel10k styles and icons, install **MesloLGS Nerd Font**:

- <https://github.com/romkatv/powerlevel10k#fonts>
- Then set your terminal font to "MesloLGS NF".

## Plugins (Zap)

- **zsh-autosuggestions** — suggests commands from history
- **zsh-syntax-highlighting** — colours valid/invalid syntax
- **supercharge** — extra key bindings and helpers
- **Aloxaf/fzf-tab** — fzf-style tab completion
- **zap-zsh/fzf** — fzf key bindings (Ctrl+R history, Ctrl+T file finder)
- **romkatv/powerlevel10k** — prompt theme

## Resources and credits

We use these as runtime dependencies (installed by Zap or the system); we do not distribute their code:

| Resource | Purpose | License |
|----------|---------|---------|
| [Zap](https://github.com/zap-zsh/zap) | Plugin manager | GPL-3.0 |
| [Powerlevel10k](https://github.com/romkatv/powerlevel10k) | Prompt theme | MIT |
| [fzf](https://github.com/junegunn/fzf) | Fuzzy finder (history, files) | MIT |
| [zsh-autosuggestions](https://github.com/zsh-users/zsh-autosuggestions) | History suggestions | MIT |
| [zsh-syntax-highlighting](https://github.com/zsh-users/zsh-syntax-highlighting) | Syntax colours | BSD-3-Clause |
| [fzf-tab](https://github.com/Aloxaf/fzf-tab) | Tab completion via fzf | MIT |
| [zap-zsh/fzf](https://github.com/zap-zsh/fzf) | fzf key bindings helper | MIT |
| [zap-zsh/supercharge](https://github.com/zap-zsh/supercharge) | Extra key bindings | MIT |
| [MesloLGS NF](https://github.com/romkatv/powerlevel10k#fonts) | Recommended font | SIL OFL |

**License note:** Only `p10k.zsh` is derived from Powerlevel10k (generated by `p10k configure`). We include the full Powerlevel10k license in [LICENSE-P10K](LICENSE-P10K). The other resources are referenced in config only; their code is fetched at runtime by Zap or installed separately.

## License

This repository is licensed under the MIT License. See [LICENSE](LICENSE).

The `p10k.zsh` configuration file was generated by [Powerlevel10k](https://github.com/romkatv/powerlevel10k)
and is subject to the Powerlevel10k MIT license. See [LICENSE-P10K](LICENSE-P10K) for the full license text
and copyright notices (Robby Russell, Ben Hilburn, Roman Perepelitsa and contributors).
